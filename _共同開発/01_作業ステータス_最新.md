# AGO_SYSTEM_MANAGER 作業ステータス（最新）

最終更新: 2026-05-13（夜）
更新者: 日本ネオン（ガント拡張・協力会社追加・GitHub Actions自動デプロイ設定）

---

## 現在のフェーズ
- **Phase**: **Phase 7 完了・GitHub Actions 自動デプロイ稼働中（push → 自動本番反映）**

## 実装済み機能（完了）

### ベース機能（NN SYSTEM MANAGER から流用済み・10機能）
1. 発注管理（資材注文）
2. ステータスフロー（受注→中国発注→工場確認→出荷準備→輸送中→納品＋キャンセル2種）
3. 顧客管理
4. 月別集計
5. 多言語（ja/zh）
6. マスターモード（ロゴ5回タップ→PIN入力）
7. プッシュ通知（VAPID）
8. 検索
9. 引継ぎメモ
10. 停滞アラート

### Phase 1 — 資材注文（中国発注強化）✅
- 中国発注ステータス 7段階（未発注/発注済/生産中/出荷待ち/輸送中/通関中/到着済）
- 到着予定日フィールド
- 現場紐づけ（スケジュール現場と連携）
- カード表示・詳細表示に反映済み

### Phase 2 — 案件パイプライン（施工項目タブ）✅
- 11段階フェーズ管理（受付→受注確定→設計中→見積中→発注準備→発注済→施工調整中→施工中→完了→請求済→キャンセル）
- 種別（看板/内装/その他）
- 担当スタッフ複数選択
- 見積金額・中国輸入フラグ
- フェーズ変更モーダル（ボタン選択式）
- localStorage キー: `ago_projects`

### Phase 3 — ANDPAD置き換え（案件詳細タブ）✅
- 写真管理（FileReader + Canvas 500KB圧縮・案件ごとに保管）
- 工事日報（記入者・日付・内容）
- チェックリスト（看板工事/内装工事テンプレート付き）
- localStorage キー: `ago_project_photos_{id}` / `ago_project_logs` / `ago_project_checklists`

### Phase 4-A — 経理・データ管理（localStorage化）✅
- 請求書管理（INV-YYYYMM-XXXX 採番）
- 発注書管理（PO-YYYYMM-XXXX 採番）
- 経費管理（月別フィルタ付き）
- 収支サマリー（月別・売上/経費/発注書/利益）
- **すべて localStorage で動作（api.php 不要）**
- localStorage キー: `ago_invoices` / `ago_purchase_orders` / `ago_expenses`

### Phase 4-B — 全データ書き出し・読み込み✅
- マスターパネル内に「全データ書き出し（.json）」ボタン
- マスターパネル内に「データ読み込み（.json）」ボタン
- 全 localStorage キーを一括エクスポート/インポート

### Phase 5 — スタッフ設定・引き渡し準備✅
- マスターパネル内にスタッフ名変更ガイドを表示
- セッティング手順書.txt を更新（Phase 1-5 完成版・LocalStorageモード）

## バックエンド系の状況

- **api.php**: KVストア（全端末sync）実装済み・gitignore対象・サーバーには手動アップが必要
  - `kv_set` / `kv_getAll` エンドポイントで localStorage を全端末共有
  - 失敗時はlocalStorageのみで動作（graceful degradation）
- **subscribe.php**: 将来のプッシュ通知登録用（現在は不要）
- **sw.js**: サービスワーカー（オフライン対応・実装済み）
- **manifest.json**: PWA設定（実装済み）

## localStorage キー一覧

| キー | 内容 |
|---|---|
| `ago_orders` | 資材注文データ |
| `ago_invoices` | 請求書データ |
| `ago_purchase_orders` | 発注書データ |
| `ago_expenses` | 経費データ |
| `ago_projects` | 案件パイプラインデータ |
| `ago_project_photos_{id}` | 案件ごとの写真（base64） |
| `ago_project_logs` | 工事日報データ |
| `ago_project_checklists` | チェックリストデータ |
| `ago_attendance` | 勤怠データ |
| `ago_gps_logs` | GPS位置ログ（最大100件） |
| `ago_instructions` | 個別指示データ |
| `ago_sch_projects` | スケジュール現場データ |
| `ago_sch_notes` | 現場申し送りデータ |

## デプロイ方式（確定）

| 項目 | 内容 |
|---|---|
| 方式 | GitHub Actions → Xserver FTP 自動デプロイ |
| トリガー | main ブランチへの push |
| 所要時間 | 約30〜60秒で本番反映 |
| FTPアカウント | `ago-deploy@system002-od.ordermade-neon.com` |
| 手動アップ | **不要**（2026-05-13以降） |

## 直近完了タスク

- ✅ 2026-04-18: `.gitignore` 整備（漏洩寸前を防止・commit 18fb743 push済）
- ✅ 2026-04-27: AGO 訪問用テンプレートに本案件を同梱
- ✅ 2026-04-27: 共同開発プロトコル整備（本フォルダ作成）
- ✅ 2026-05-02: Phase 1-5 全機能実装完了（localStorage完全移行）
- ✅ 2026-05-02: セッティング手順書.txt 更新（Phase 1-5 完成版）
- ✅ 2026-05-02: 全データ書き出し/読み込み機能実装
- ✅ 2026-05-03: 専務・常務監査 NO-GO 指摘事項 #1-#10 全修正完了（詳細は下記）
- ✅ 2026-05-03: Phase 6-A ANDPADボード実装（週次カレンダー・スタッフ×案件配置）
- ✅ 2026-05-03: Phase 6-B 図面管理実装（画像/PDFアップロード・Canvas書き込み・ピン指摘）
- ✅ 2026-05-03: Phase 6-C 見積書・積算実装（EST採番・明細・発注書変換）
- ✅ 2026-05-03: Phase 6-D 案件別チャット実装（LINE風UI・@メンション・既読確認）
- ✅ 2026-05-03: Phase 6-E PDF出力実装（jsPDF・請求書/見積書/日報/チェックリスト）
- ✅ 2026-05-03: Phase 6-F IndexedDB写真実装（localStorage→IDB移行・容量問題解消）
- ✅ 2026-05-03: Phase 6-G 電子黒板実装（写真+黒板Canvas合成・施工記録写真出力）
- ✅ 2026-05-03: Phase 6-H 協力会社・職人管理実装（マスターパネル内管理・案件紐づけ）
- ✅ 2026-05-03: 専務・常務 Phase 6 監査 GO（Critical 5件修正後・commit 192f6da push済み）
- ✅ 2026-05-13: 実機確認（全9タブ正常・Basic認証 system002/nihonneon・開発者PIN 1651）
- ✅ 2026-05-13: ユーザー名実名化（AGO現地対応済み・config.js更新）
- ✅ 2026-05-13: 全端末sync機能実装（api.php kv_store経由・失敗時はlocalStorage動作）
- ✅ 2026-05-13: serve.bat を PHP built-in server に変更（python→php -S localhost:8080）
- ✅ 2026-05-13: .htaccess に manifest.json / sw.js Basic認証除外（PWAホーム追加対応）
- ✅ 2026-05-13: favicon を data URI で追加（コンソール404エラー解消）
- ✅ 2026-05-13: タブスクロール・PINモーダル・CSV取り込み位置の修正（commit 9cec0fa）
- ✅ 2026-05-13: スケジュールタブのタップ修正・CSVファイル選択iOS対応（commit 1934663）
- ✅ 2026-05-13: 資材注文カードに現場名バッジ追加（commit 5eec044）
- ✅ 2026-05-13: ガントチャートに日毎/時間毎トグル＋スタッフD&D並び替え復元（commit a6d71c3）
- ✅ 2026-05-13: 日毎ガントに協力会社を追加・D&D対応（commit 8161b03）
- ✅ 2026-05-13: タイムライン24時間表示・日またぎ予定を翌日に継続表示（commit bbb30ff）
- ✅ 2026-05-13: GitHub Actions FTP自動デプロイ設定（commit f024a29 / f3a45a8）→ 動作確認済み

## 監査対応修正ログ（2026-05-03）

| # | 指摘 | 修正内容 | 対象 |
|---|---|---|---|
| #1 | MASTER_PIN ハードコード (C-1) | APP_CONFIG.masterPin から動的取得に変更 | index.html 行1311 |
| #2 | ログイン機構なし (C-3) | ユーザー選択モーダル + PIN認証 + applyFeatureVisibility修正 + switchTab権限チェック追加 | index.html |
| #3 | displayName → name ガイド誤記 | マスターパネル内ガイドを name: に修正 | index.html 行487-488 |
| #4 | PWAアイコン未存在参照 | manifest.json icons を空配列に、apple-touch-icon削除 | manifest.json / index.html |
| #5 | importAllData バックアップなし (C-2) | インポート前に exportAllData() 自動実行 + 2段階確認 | index.html |
| #6 | 写真 QuotaExceededError 未対応 | lsSave を try-catch で囲む | index.html |
| #7 | created_by ハードコード 'onodera' | localStorage.getItem('ago_current_user') に変更（3箇所: saveInvoice/saveExpense/saveSchedule） | index.html |
| #8 | USERS 定数二重管理 | APP_CONFIG.users から自動生成に変更 | index.html 行1295-1297 |
| #9 | 手順書 PHP記述矛盾 (M-1) | PHP手順を「将来オプション」セクションに移動、冒頭にLocalStorageモード明記 | セッティング手順書.txt |
| #10 | 作業ステータス過大表記 (m-1) |「全機能実装完了→引き渡し準備段階」→「主要機能実装完了・残課題修正中」に修正 | 01_作業ステータス_最新.md |

## 進行中タスク

（なし）

## 未着手タスク

1. **【必須】api.php をサーバーへ手動アップ**（Xserverファイルマネージャーまたは FTP）
2. ~~【確認】菅野社長へのマスターPIN（kanno0）伝達確認~~ → ✅ 2026-05-13 伝達済み
3. AGO スタッフからのフィードバック収集・改修対応（実装後）
4. PDF 日本語フォント対応（Noto Sans JP 埋め込み）— 現状は文字化けリスクあり
5. IndexedDB 写真のバックアップ/リストア対応（exportAllData が写真を含まない問題）
6. 菅野社長へのヒアリング（実データ入力・実スタッフ名での動作確認）

## 申し送り事項（引き渡し先 = AGO / 菅野社長）

### 重要
- データはすべてブラウザの localStorage に保存される
  - バックアップ: マスターパネル →「全データ書き出し」→ .json ファイルを定期保管
  - 復元: マスターパネル →「データ読み込み」→ .json ファイルを選択
- ブラウザのキャッシュ・localStorage をクリアするとデータが消える（定期バックアップ必須）
- api.php / PHP サーバーは不要（ブラウザで index.html を直接開くだけで動作）
- VAPID 鍵は config.js に記載 → 機密扱い・GitHub に push しない
- `github.com/nihonneon2025/ago-system-demo` は公開リポジトリ → 実スタッフ名・実顧客名をコミットしない

### スタッフ名の変更方法
1. `config.js` をテキストエディタで開く
2. `users` セクションの `name: 'スタッフ01'` を実名に変更
3. `masterPin: 'kanno0'` を任意の PIN に変更（推奨）
4. 保存してブラウザをリロード

### マスターモードの入り方
- 画面左上の「AGO」ロゴを5回連続タップ → PINコード入力

## 判断履歴

- フロントエンド単一HTML + PWA構成は変更しない方針（維持）
- DB はすべて localStorage（将来 api.php + SQLite 移行は別フェーズ）
- 多言語は ja/zh の2言語で当面継続（中国側スタッフのため）
- api.php は現在コール不要だが、将来のバックエンド拡張用にファイルは残存

## 既知の課題

1. localStorage はブラウザごと・端末ごとに独立する（複数端末での同期は将来課題）
2. 写真データ（base64）が増えると localStorage 容量を圧迫する可能性（5〜10MB上限）
3. VAPID 鍵の管理場所が config.js → GitHub 公開リポジトリへのコミット禁止
4. ブラウザのプライベートモード・キャッシュクリアでデータが消えるリスク
5. PDF出力の日本語フォント非対応（jsPDF標準フォントのみ・文字化けリスク）
6. IndexedDB写真がexportAllDataに含まれない（写真バックアップ不完全）
7. IndexedDB cursorスキャンが全件スキャン（写真数増加時の遅延リスク）

## 移植元情報

- 移植元: 日本ネオン株式会社 AI 組織（小野寺社長）
- 移植実施: 2026-04-27 午前
- 移植担当: 日本ネオン本部長AI
- Phase 1-5 実装: 2026-05-02 日本ネオン受託事業部長AI
- 移植先: AGOグループ AI 組織（菅野社長）

## 2026-05-15 作業ログ

- GitHub リポジトリを **Private** に変更（社外秘コード保護）
- GitHub Actions 自動デプロイを **再開**（push → Xserver 自動反映・約30秒）
- **両拠点（AGO現地・自宅）どちらで作業しても GitHub push で本番反映される体制が完成**
- 作業ルール: 開始前「pull して」→ 終了後「push して」とチャットに書くだけ
